# Vercel deployment trigger

This commit is intentionally created from the authenticated GitHub account to restore a deployable author identity for Vercel.

It does not change runtime behavior. The deployment should rebuild from `main` using the current production configuration.
