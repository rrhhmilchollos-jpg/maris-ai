[build]
  command = "pnpm install --no-frozen-lockfile && pnpm --filter @workspace/appforge run build"
  publish = "artifacts/appforge/dist"

[[redirects]]
  from = "/api/*"
  to = "https://maris-ai-backend.onrender.com/api/:splat"
  status = 200
  force = true
