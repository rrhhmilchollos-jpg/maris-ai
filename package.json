{
  "name": "workspace",
  "version": "0.0.3",
  "license": "MIT",
  "packageManager": "pnpm@10.11.0",
  "engines": {
    "node": ">=20",
    "pnpm": ">=10"
  },
  "scripts": {
    "validate-config": "tsx scripts/validate-config.ts",
    "build": "pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "vercel": {
    "outputDirectory": "artifacts/appforge/dist",
    "buildCommand": "cd artifacts/appforge && rm -rf dist && npx vite build --config vite.config.ts && node prerender.mjs"
  },
  "devDependencies": {
    "execa": "^9.6.1",
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  },
  "dependencies": {
    "esbuild": "^0.28.0"
  }
}
