{
  "outputDirectory": "artifacts/appforge/dist",
  "buildCommand": "pnpm --filter @workspace/api-client-react build && pnpm --filter appforge build",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
